const Display = (props) => {
    return(
        <div>
            <h2>{props.name}</h2>
            <p>Turning {props.age} years old on {props.Date}</p>

        </div> 
            
        )   
}

export default Display;